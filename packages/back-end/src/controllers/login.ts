import { Request, Response } from 'express';
import { CreateAppOptions } from '..';

export default ({ tokenService, passwordService }: CreateAppOptions) =>
  (_req: Request, _res: Response) => {
    //
  };
